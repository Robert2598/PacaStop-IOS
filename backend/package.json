{
  "name": "pacastop-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "PacaStop AI recovery-mentor proxy. Holds the Anthropic key, verifies Clerk sessions, gates premium, rate-limits, and streams Claude responses. Never receives any financial data.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.95.0"
  }
}
